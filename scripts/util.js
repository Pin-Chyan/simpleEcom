// const axios = require('axios');

// exports.generateText = (name, age) => {
//     return `${name} (${age} years old)`;
// }

// exports.searchGame = async (title) => {
//     // let games = await axios.get("https://www.cheapshark.com/api/1.0/games?title=" + title);
    
//     // console.log(Object.keys(games.data).length);
//     var myObject = {'name':'Sherlock', 'address':'221b Bakerstreet','city': 'London'}

//     var count = Object.keys(myObject).length;
//     console.log("search nbr: " + count);
//     return count
// }